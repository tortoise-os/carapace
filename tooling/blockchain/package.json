{
  "name": "@carapace/tooling-blockchain",
  "private": true,
  "version": "0.1.0",
  "description": "Sui blockchain interaction scripts for Carapace",
  "type": "module",
  "scripts": {
    "add-liquidity": "bun run src/add-liquidity.ts",
    "check-tx": "bun run src/check-tx.ts",
    "create-pool": "bun run src/create-pool.ts",
    "debug-pool": "bun run src/debug-pool.ts",
    "query-pool": "bun run src/query-pool.ts",
    "test-swap": "bun run src/test-swap.ts"
  },
  "dependencies": {
    "@carapace/sdk": "workspace:*"
  }
}
